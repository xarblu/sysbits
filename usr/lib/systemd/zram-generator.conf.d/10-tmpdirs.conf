# /tmp on zram
[zram1]
zram-size = ram / 4
compression-algorithm = zstd
mount-point = /tmp
fs-type=ext4
options = X-mount.mode=1777

# /var/tmp (portage) on zram
[zram2]
zram-size = ram * 0.8
compression-algorithm = lz4
mount-point = /var/tmp
fs-type=ext4
options = X-mount.mode=1777
