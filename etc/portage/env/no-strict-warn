# disable some strict warnings
# mostly in old C code
NOWARN="${NOWARN} -Wno-implicit-function-declaration"

# became an error in clang-20 (I think)
NOWARN="${NOWARN} -Wno-missing-template-arg-list-after-template-kw"

# e.g. assigning to 'char *' from 'const char *' discards qualifiers
# fairly common since glibc-2.43
NOWARN="${NOWARN} -Wno-incompatible-pointer-types-discards-qualifiers"

# seems to be rare and when it occurs it's on packages
# I don't really care about
NOWARN="${NOWARN} -Wno-incompatible-pointer-types"

# apply
EXTRA_CFLAGS="${NOWARN}"
EXTRA_CXXFLAGS="${NOWARN}"
EXTRA_FCFLAGS="${NOWARN}"
EXTRA_F77FLAGS="${NOWARN}"
EXTRA_LDFLAGS="${NOWARN}"
