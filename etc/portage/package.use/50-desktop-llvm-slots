# llvm slots different from the stable default
# usually only to select unstable slots
# quse --exact --installed --quiet --verbose llvm_slot_22 | perl -pe 's/^(.*)$/>=\1 LLVM_SLOT: -* 22/'

# held back by intel-graphics-compiler
# but that's the only consumer so whatever
dev-libs/intel-vc-intrinsics LLVM_SLOT: -* 16

# unstable slots
sys-kernel/cachyos-kernel LLVM_SLOT: -* 22
